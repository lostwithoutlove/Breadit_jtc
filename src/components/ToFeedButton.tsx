"use client";
import { FC, useEffect, useRef } from "react";
import { useSession } from "next-auth/react";

interface ToFeedProps {}

const ToFeedButton = async ({}) => {
  return (
    <div>
      <p>ToFeed</p>
    </div>
  );
};

export default ToFeedButton;
